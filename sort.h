#ifndef SORT_H
#define SORT_H

#include <stddef.h>
#include <stdio.h>

/* Helper function provided by project */
void print_array(const int *array, size_t size);

/* Sorting function */
void bubble_sort(int *array, size_t size);

#endif /* SORT_H */
